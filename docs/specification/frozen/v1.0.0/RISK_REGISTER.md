# MediTriageAI v1.0.0 — Risk Register

**Specification version:** v1.0.0-FROZEN

**Governing rule:** Unresolved risks remain unresolved. No risk is downgraded without repository evidence or an approved Change Request.

---

| # | Risk | Status | Likelihood | Impact | Mitigation | Residual Uncertainty |
|---|---|---|---|---|---|---|
| 1 | **Regex-label circularity** — specialty labels inferred by regex (4/13 datasets) may correlate with regex-based severity heuristic by construction, both being keyword-based | FACT-grounded risk | Plausible — structural coupling exists by design | High — inflated metric correlation between tasks could mislead reviewers | Stratified reporting by label-provenance tier (`DIRECT`/`MAPPED`/`INFERRED`); never pool without this breakdown | Magnitude of circularity effect on headline metrics is UNKNOWN until provenance-stratified evaluation is run |
| 2 | **Severity sparsity** — only ~1.3% of dataset rows carry severity labels, and 63% of those come from `fedmml_ed_triage` (LLM-generated, Category C) | FACT | Certain — structural property of current dataset | Critical — severity task may be dominated by synthetic signal | Report synthetic share alongside every severity result; sensitivity analysis excluding synthetic rows (ADR-005) | Whether the 37% non-synthetic severity labels alone provide sufficient signal for a defensible severity model is UNKNOWN |
| 3 | **NEISS dominance** — NEISS constitutes ~70% of total dataset volume, is injury-only | FACT | Certain — structural property | High — model may systematically underperform on non-injury presentations | Per-class recall reporting, not just macro-averaged accuracy | Exact recall gap between injury and non-injury classes is UNKNOWN until per-class evaluation is run |
| 4 | **512-token truncation drops late red flags** — truncation mechanism exists (FACT), late-appearing symptoms may be silently discarded | FACT (mechanism); rate UNKNOWN | Plausible | High — missed safety-critical information | Truncation-rate metric reported; deterministic tail-window red-flag re-scan considered (FUTURE-OPTIONAL, cheap, does not require retraining) | Truncation rate on actual dataset is UNKNOWN; interaction with red-flag recall is UNKNOWN |
| 5 | **`src/`↔`meditriage/` migration stalls or silently changes numbers** — active cross-boundary dependencies exist between `src/` and `meditriage/` (FACT) | Structural risk | Plausible during migration phase | Critical — silent numerical divergence could invalidate published results | Hard gate (SPEC-03): Priority-1 modules must reach verified numerical parity before any frozen-spec paper results run; parity tests required before any caller switch; `src/`-sourced numbers footnoted "(legacy harness)" until verified | Whether migration introduces subtle numerical differences is inherently UNKNOWN until parity tests are run per-module |
| 6 | **Backbone naming implies larger/different models** — `XLMRobertaLargeModel` loads `xlm-roberta-base`; `IndicBertModel` loads `google/muril-base-cased` | CONTRADICTED, now resolved by policy (ADR-002) | N/A — resolved by Option B naming policy | High if unresolved — reviewer could assume a different model capacity | Option B naming policy (SPEC-05); prohibited-substitution lint (FR-MODEL-02) | Lint enforcement (FR-MODEL-02) is MISSING; risk remains active until lint is implemented at Gate 6/7 |
| 7 | **Paper claims multi-anchor mean embedding init; code does single-anchor clone** — `docs/PAPER_RESULTS_DRAFT.md:178-180` contradicted by `src/vocab_injection.py:56-68` | CONTRADICTED, now resolved by decision (ADR-003) | N/A — resolved by freezing single-anchor clone as canonical | Medium — reviewer catching the discrepancy before correction would damage credibility | SPEC-05 canonical text corrected; paper draft correction required before submission | Paper draft has not yet been corrected — correction is a post-freeze editorial task |
| 8 | **Red-flag mechanism has unknown, unaudited recall** — 8 hardcoded keywords with no ground-truth evaluation dataset (FACT) | FACT (no dataset exists) | UNKNOWN — untested | Critical — safety-critical mechanism is unevaluated | Adversarial red-flag evaluation dataset (SPEC-07): strata DECIDED, construction method/adjudication/size TBD | No red-flag recall/precision number can be reported until the dataset exists; must appear as-is in paper limitations |
| 9 | **Prior training artifact may be severely English-dominant** — conflicting reports about language distribution (~99.42% English figure is UNVERIFIED) | **UNKNOWN, unverified, conflicting reports** | UNKNOWN | Critical if true — invalidates any Hinglish/multilingual claim based on prior training | DATASET-GATE-01 (structural fix); pending language-distribution audit (Gate 1, first post-freeze task) | Status remains UNKNOWN until Gate 1 audit is actually executed; the ~99.42% figure is assumed neither true nor false |
| 10 | **"Synthetic" data pooled without distinguishing evidentiary strength** — prior spec versions used "synthetic" as undifferentiated bucket | FACT — ambiguity existed in prior spec drafts | Certain — structural terminology problem | Medium — undermines paper's evidentiary claims if deterministic transforms, templates, and LLM generations are conflated | Text-provenance taxonomy (`SOURCE`/`A`/`B`/`C`, ADR-009); retroactive dataset audit required to assign categories | Retroactive audit of every AUXILIARY dataset's augmentation has not been performed |
| 11 | **"Hinglish support" claims risk overstating a controlled phonetic layer as general understanding** | FACT — gap identified between what exists (deterministic phonetic/orthographic variation) and what "Hinglish understanding" implies (compositional/code-switched semantic understanding) | Plausible — natural language inflation risk | High — overclaim in paper/UI would mislead reviewers | Multilingual Robustness Audit Matrix (Section 6): 20 dimensions, most at UNKNOWN/PARTIAL; binding paper-language constraint (SPEC-06) | Per-dimension audit (Gate 4) has not been performed; most dimension statuses remain UNKNOWN |
| 12 | **E-PATH results could be presented alongside validated baselines, overstating its status** | Structural risk, not yet materialized | Plausible if reporting discipline is not enforced | Medium — conflating experimental and validated results misleads reviewers | Separate-subsection reporting rule (SPEC-05): E-PATH never pooled with four production backbones until it has a complete, statistically-validated DGX training campaign | E-PATH has not yet undergone a full DGX training campaign |
| 13 | **Arbitrary safety thresholds could be mistaken for clinical validation** — e.g., a proposed ECE ≤ 0.08 had no cited basis | Structural risk | Plausible if thresholds are introduced without justification | High — fabricated-looking threshold in a clinical paper damages credibility | TBD-by-default rule (ADR-012): no numerical safety threshold is normative unless supported by an authoritative source or documented empirical methodology | All safety-related thresholds remain TBD / NOT CLINICALLY VALIDATED |
